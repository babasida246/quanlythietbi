prefer-workspace-packages=true
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
# Prevent pnpm from creating broken bin scripts in workspace sub-packages
# when using hoisted node-linker (bins resolved from root node_modules)
hoist-workspace-packages=false